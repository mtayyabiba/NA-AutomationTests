exports.map = { 
    reason: element.all(by.id('reason'))  ,
    ticketNumber: element.all(by.id('ticket-number'))  ,
    email: element.all(by.id('email')),
    countryCode: element.all(by.id('country-code')) ,
    contactNo: element.all(by.id('contact-no')) ,
    findTicketButton: element.all(by.id('find-ticket')),
    cancelButton: element.all(by.id('submit-reason')),
    copyButton: element.all(by.id('copy-code'))
}